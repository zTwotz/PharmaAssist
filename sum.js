/**
 * Tính tổng hai số
 * @param {number} a 
 * @param {number} b 
 * @returns {number}
 */
function sum(a, b) {
    return a + b;
}

// Ví dụ sử dụng
const num1 = 5;
const num2 = 10;
console.log(`Tổng của ${num1} và ${num2} là: ${sum(num1, num2)}`);
